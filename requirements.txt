# pip-audit: no known vulnerabilities

# LLM provider — current Google Gemini SDK (replaces deprecated google-generativeai)
google-genai==2.8.0

# Data validation & settings
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2

# Optional integrations: Telegram digest (httpx) and Google Sheets export (gspread)
httpx==0.28.1
gspread==6.2.1
google-auth==2.55.0

# Testing
pytest==9.1.0
pytest-asyncio==1.4.0
